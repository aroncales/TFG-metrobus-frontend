import React from 'react';

export default function Stops() {
  return (
    <div id="panel-stops" className="p-4" style={{ paddingBottom: 'var(--nav-h,56px)' }}>
      <h1 className="text-lg font-semibold mb-2">Paradas</h1>
      <p className="text-sm text-gray-700">Listado/buscador de paradas por nombre o ID (pendiente).</p>
    </div>
  );
}
